# Changa brand assets

Place the official logo in one (or both) of these files:

- `logo-changa.svg` (preferred)
- `logo-changa.png` (fallback)

The app will attempt to load the SVG first and then the PNG.
